import { useState } from 'react';
export default function HomePage({onCreateTeam}){const [teamName,setTeamName]=useState('');const [player,setPlayer]=useState('');const [players,setPlayers]=useState([]);const add=()=>{if(player){setPlayers([...players,player]);setPlayer('')}};return <div className='page'><div className='card'><h1>Coach Tactics Board</h1><input placeholder='Team Name' value={teamName} onChange={e=>setTeamName(e.target.value)}/><div className='row'><input placeholder='Player Name' value={player} onChange={e=>setPlayer(e.target.value)}/><button onClick={add}>Add</button></div><div className='players'>{players.map((p,i)=><span key={i} className='pill'>{p}</span>)}</div><button className='main' onClick={()=>onCreateTeam({name:teamName,starters:players.slice(0,7).map((p,i)=>({id:i,name:p,x:80+i*70,y:100+(i%2)*120})),subs:players.slice(7)})}>Select Team</button></div></div>}
